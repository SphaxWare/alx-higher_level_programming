#!/usr/bin/python3
def best_score(a_dictionary):
    if not a_dictionary:
       return None
    max_v = max(a_dictionary.values)
    for k , v in a_ictionary.items():
        if v == max_v:
            return key
    return None 
